/*Primeiro arquivo em linguagem c.
Para indicar iremos importar a biblioteca stdio (std-standard(padrão) io-input output
(entrada e saida). esta biblioteca possui os comandos para interação com oteclado e monitor*/
#include <stdio.h>
/* iniciar a estrutura do codigo para a mensagem hello word, usando a função main (principal),pois o compilador c ira procurar
esta função para dar execução no nosso codigo.*/
int main(){
printf("hello word\n");
return 0;  
}
